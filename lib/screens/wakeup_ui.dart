import 'package:flutter/material.dart';

class wakeup_ui extends StatelessWidget {
  const wakeup_ui({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}